<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="invoice_supplier_form_with_fiscal_position_required" model="ir.ui.view">
        <field name="name">Supplier Invoice Form With Fiscal Position Required</field>
        <field name="model">account.invoice</field>
        <field name="inherit_id" ref="account.invoice_supplier_form"/>
        <field name="arch" type="xml">
            <field name="fiscal_position_id" position="attributes">
                <attribute name="attrs">{'required': [('state', '=', 'draft')]}</attribute>
            </field>
        </field>
    </record>

    <record id="invoice_form_with_fiscal_position_required" model="ir.ui.view">
        <field name="name">Customer Invoice Form With Fiscal Position Required</field>
        <field name="model">account.invoice</field>
        <field name="inherit_id" ref="account.invoice_form"/>
        <field name="arch" type="xml">
            <field name="fiscal_position_id" position="attributes">
                <attribute name="attrs">{'required': [('state', '=', 'draft')]}</attribute>
            </field>
        </field>
    </record>

</odoo>
