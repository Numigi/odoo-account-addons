<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="action_reconcile_and_open_payment" model="ir.actions.server">
        <field name="name">Apply Credits And Register Payment</field>
        <field name="model_id" ref="model_account_invoice"/>
        <field name="binding_model_id" ref="model_account_invoice"/>
        <field name="state">code</field>
        <field name="code">action = records.reconcile_and_open_payment()</field>
    </record>

</odoo>
